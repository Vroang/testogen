# PLAN — план разработки

Работаем строго пошагово. После каждого шага — проверка на телефоне.
PLAN не меняется. Если нужно что-то добавить — это VISION-2, отдельно.

## Шаг 0. Скелет + иконка + главный экран
- Проект Kotlin + Compose (Material 3), тема (цвета, шрифты).
- Кастомная иконка приложения (adaptive icon, шапочка 🎓 в чёрно-жёлтых
  цветах на светлом фоне).
- Главный экран в стиле референса:
  - шапка с иконкой-шапочкой, «Конструктор тестов / для учителей»,
    шестерёнка справа (пока не нажимается);
  - заголовок «Настройте структуру теста», подзаголовок;
  - поле «Тематика теста» (можно вводить, но пока не сохраняется);
  - два ползунка с кнопками «−/+»: вариантов (1–40, по умолч. 1),
    вопросов (5–40, по умолч. 10);
  - чипсы сложности (Все / Лёгкие / Средние / Сложные) — работают локально;
  - два чекбокса («вопросы с подвохом», «показывать ответы отдельно») —
    работают локально;
  - инфо-карточка «Будет собрано: X × Y = Z»;
  - большая teal-кнопка «✨ Собрать тест» → пока Toast «Скоро».
- Плавный fade-in при открытии экрана.
- APK собирается, ставится, открывается.

## Шаг 1. Ручной ввод вопроса
- Экран «Новый вопрос»: текст вопроса, 4 поля вариантов, отметка
  правильного, выбор сложности, чекбокс «С подвохом».
- Кнопка «Сохранить» — сохраняет в Room (таблица questions).
- Проверка: вводится, сохраняется.

## Шаг 2. Банк вопросов
- Экран «Банк вопросов» (аналог «Шаг 1» на референсе):
  - три вкладки: «Ручной ввод» / «Файл» / «Интернет»
    (работает только «Ручной ввод»; две другие — заглушки);
  - список сохранённых вопросов, удаление каждого;
  - кнопка «Очистить».
- Проверка: список, добавление, удаление, очистка.

## Шаг 3. Сборка черновика теста
- Кнопка «Собрать тест» берёт вопросы из банка, фильтрует по
  сложности и «с подвохом», распределяет по вариантам: каждый
  вариант максимально уникален.
- Открывается экран черновика со списком вариантов и вопросов.
- Проверка: черновик формируется по ползункам и фильтрам.

## Шаг 4. Замена вопроса в черновике
- У каждого вопроса — кнопка «Заменить».
- Диалог: причина замены (свободный ввод + подсказки), источник
  нового вопроса (сгенерировать / вписать вручную).
- Причина сохраняется в Room (таблица replacement_reasons).
- Проверка: замена работает, причина сохраняется.

## Шаг 5. Экспорт теста в Word
- Кнопка «Скачать файл» — DOCX: все варианты в одном файле, каждый
  с новой страницы.
- Сохранение в папку / «Поделиться».
- Проверка: DOCX открывается, структура верна.

## Шаг 6. Экспорт ответов в Word
- Кнопка «Файл с ответами» — второй DOCX: для каждого варианта
  компактная таблица «№ | Ответ».
- Сохранение / «Поделиться».
- Проверка: файл открывается, таблицы корректны.

## Шаг 7. Настройки + API-ключ + выбор модели + каскад
- Экран настроек:
  - поле для API-ключа OpenRouter (точками);
  - кнопка «Проверить ключ» — минимальный запрос (1 токен);
  - при успехе подгружается список моделей из
    openrouter.ai/api/v1/models;
  - выпадающее меню «Модель»;
  - у каждой модели метка «Бесплатно» / цена за 1M токенов;
  - фильтр: «Только бесплатные» (по умолч. вкл.) / «Все модели»;
  - по умолчанию — бесплатная модель (deepseek :free, если есть);
  - раздел «Каскад моделей»: 1) основная; 2) резервные бесплатные
    (2–3 популярные :free); 3) резервные платные (DeepSeek V4 Flash,
    затем DeepSeek V4 Flash 0423);
  - кнопка «Проверить каскад» — 1 токен на каждую модель;
  - при 429 (лимит) — сразу переход к платным, без трат попыток.
- Ключ, модель и каскад — в DataStore.
- Проверка: ключ вводится, «Проверить» работает, список подгружается,
  каскад настраивается, сохраняется после перезапуска.

## Шаг 8. ИИ-генерация по теме
- Вкладка «Интернет» в банке: тема, количество вопросов, сложность.
- Сборка system prompt из ai_instructions/ (см. Шаг 11).
- Запрос в OpenRouter с каскадом → вопросы с 4 вариантами → в банк.
- Проверка: по теме приходят вопросы, при ошибке/лимите — каскад.

## Шаг 9. Загрузка PDF учебника
- Вкладка «Файл»: выбор PDF, извлечение текста (PdfBox-Android),
  превью, указание темы.
- Проверка: PDF читается, текст извлекается.

## Шаг 10. Генерация вопросов из PDF
- Извлечённый текст + тема → OpenRouter (с каскадом) → вопросы в банк.
- Проверка: вопросы соответствуют содержанию учебника.

## Шаг 11. Папка инструкций для ИИ
- Внутри приложения приватная папка ai_instructions/:
  - system.txt — базовая инструкция для модели;
  - avoid.txt — сжатые правила «чего не делать»;
  - subjects.txt — предметные нюансы по классам.
- Экран «Инструкции для ИИ» — простой редактор трёх файлов.
- При каждом запросе к ИИ файлы собираются в system prompt.
- Проверка: экран открывается, файлы сохраняются, запрос уходит
  с их содержимым.

## Шаг 12. Сжатие причин замен + очистка инструкций
- Причины замены НЕ отправляются сырым списком.
- Кнопка «Обновить инструкции»: причины → 10–30 правил → avoid.txt.
- Автоматически предлагается раз в 15 замен.
- Экран «Инструкции для ИИ»:
  - редактор трёх файлов;
  - счётчик правил;
  - при > 30 — предупреждение «подчистите»;
  - кнопка «Очистить только причины замен» (avoid.txt);
  - кнопка «Очистить ВСЕ инструкции» (с подтверждением);
  - кнопка «Сбросить до заводских».
- Проверка: сжатие работает, avoid.txt компактный, кнопки очистки
  работают, генерация после очистки проходит.

## Шаг 13. Полировка
- Плавные анимации переходов, оптимизация Room под 40×40.
- Проверка: всё плавно, без лагов и подёргиваний.
