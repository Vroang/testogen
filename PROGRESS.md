# PROGRESS

## Текущий шаг: 31.1 — «Восстановить из облака» тянет и вопросы, и причины
## Статус: собран, тег v47 отправлен, ждёт проверки пользователем

## Что сделано
- ДИАГНОСТИКА: кнопка «Восстановить из облака» вызывала только
  TextbookRepository.restoreFromCloud (учебники + параграфы).
  Pull для questions и replacement_reasons в шаге 31 не
  существовало — был только push. Поэтому вопрос из веб-версии
  не появлялся на телефоне.
- ФИКС (QuestionSyncRepository):
  - pullQuestionsAndReasons(): читает questions и
    replacement_reasons из Supabase (учёт RLS — только свои);
    без дублей: сначала сопоставление по cloudId, затем по
    тексту + правильному ответу (для записей без cloudId
    cloudId просто связывается с локальной копией); новые
    вставляются со syncStatus = "synced";
  - restoreAll(): учебники (как раньше) + вопросы + причины;
    возвращает Triple счётчиков;
  - локальные pending-вопросы не удаляются и не теряются;
    DTO смягчены к nullable — вопросы, созданные в вебе с
    пустыми полями, читаются корректно;
  - pullAllIfStale(): автоподтягивание при старте приложения —
    не чаще одного раза в 5 минут (метка времени в памяти);
  - mapError с понятным сообщением для RLS-ошибок.
- UI: Настройки → «Восстановить из облака» теперь зовёт
  restoreAll; во время восстановления — полоса прогресса и
  «Восстанавливаем…»; итоговый Toast: «Восстановлено: N
  учебников, M вопросов, K причин».
- app/build.gradle.kts: versionCode = 47,
  versionName = "0.31.1-step31fix".
- Push-синхронизация, логика учебников, каскад,
  автообновление — не тронуты. Новых библиотек нет.

## Известные проблемы
—
## Заметки
- Путь к локальному APK:
  C:\Users\Ivan\Desktop\TestGenerator\apk\ТестоГен-v0.31.1-step31fix.apk
- Версионирование: versionCode монотонно растёт (шаг 31.1 = 47).
- Автопубликация: тег v47 → GitHub Actions → релиз.
- VISION.md и PLAN.md не менялись.
