# PROGRESS

## Текущий шаг: 24 — быстрые фиксы: FAB «Банк», лимиты 50, порции 10, фильтр дублей
## Статус: собран, ждёт проверки пользователем

## Что сделано
- ЧАСТЬ 1 — FAB на главном экране: вместо «+» теперь
  ExtendedFloatingActionButton с иконкой «книга» и подписью «Банк»,
  teal-заливка, белый текст, ведёт в question_bank как раньше.
  Иконка MenuBook нарисована кодом (ImageVector с официальным
  Material-путём) — БЕЗ подключения material-icons-extended
  (новых библиотек нет).
- ЧАСТЬ 2 — лимит количества вопросов 20 → 50:
  - вкладка «Интернет» (банк): кнопка «+» активна до 50;
  - вкладка «Файл»: то же самое;
  - при count > 30 под счётчиком появляется серая подпись
    «Генерация может занять несколько минут» (12sp,
    onSurfaceVariant).
  - Каскад моделей и парсер не тронуты: сохраняем столько,
    сколько модель вернула.
- ЧАСТЬ 3 — добор порциями 5 → 10:
  - «Добрать и собрать» (главный экран) генерирует порциями по 10,
    последняя порция — остаток (счётчик порций привязан к
    сгенерированному количеству, не к сохранённому);
  - прогресс и тексты («Добрано X из Y», кнопка «Остановить»)
    без изменений; логика остановки и ошибок не менялась.
- ЧАСТЬ 4 — фильтр ИИ-дублей (Db.kt):
  - normalizeQuestionText(): нижний регистр + удаление всех
    пробелов + срез хвостовых «?», «!», «.»;
  - QuestionDao.insertUnique(): сверяет каждый новый вопрос с
    нормализованными текстами банка (и с уже добавленными в этой
    же партии); точное совпадение — вопрос отбрасывается;
    в банк пишется ИСХОДНЫЙ текст без изменений;
  - подключён ко всем трём потокам генерации: по теме (Интернет),
    из файла (PDF/DOCX/TXT) и добор на главном экране;
  - ручное добавление и замена вопроса — БЕЗ фильтра (как раньше);
  - Toast-итоги: «Добавлено X вопросов (отброшено Y дублей)»;
    если сохранилось 0 — «Все сгенерированные вопросы уже есть
    в банке»; без дублей — прежний текст «Добавлено X вопросов»;
  - защита от цикла: в доборе число порций ограничено запрошенным
    количеством (generate-лимит), дубли НЕ запускают доп. порции.
- app/build.gradle.kts: versionCode = 33, versionName = "0.24-step24".
- Логика черновика, экспорта, настроек, автообновления и
  .github/workflows — не тронуты.

## Известные проблемы
—
## Заметки
- Путь к APK: C:\Users\Ivan\Desktop\TestGenerator\apk\ТестоГен-v0.24-step24.apk
- Версионирование: versionCode монотонно растёт (шаг 24 = 33).
- Тег v0.24 НЕ создавался (по заданию): пользователь сначала ставит
  APK локально и проверяет; автосборка релиза — отдельной командой.
- На GitHub исправлен workflow: имена файлов релиза латиницей
  (Testogen-<тег>.apk) — GitHub вырезает кириллицу из имён ассетов
  (коммит 7ec624e, вне рамок шага 24).
- Новых библиотек нет. VISION.md и PLAN.md не менялись.
- Следующий шаг — журнал ошибок генерации (по плану).
