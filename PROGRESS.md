# PROGRESS

## Текущий шаг: 26 — VISION-2: без превью в «Файле» + мультивыбор сложностей
## Статус: собран, тег v36 отправлен, ждёт проверки пользователем

## Что сделано
- ЧАСТЬ 1 — вкладка «Файл»: блок «Превью текста» удалён целиком
  (заголовок, серая карточка со скроллом, локальная переменная
  previewText). Само извлечение текста из PDF/DOCX/TXT НЕ тронуто —
  оно нужно для генерации. Остальная форма без изменений: имя файла,
  страницы/символы, «×», тема, количество, сложность, кнопка.
- ЧАСТЬ 2 — мультивыбор сложностей:
  - чипсы «Все | Лёгкий | Средний | Сложный» заменены на три
    «Лёгкий / Средний / Сложный» с мультивыбором; по умолчанию
    включены все три; включённая — teal-заливка/белый текст,
    выключенная — белая с рамкой (ChoiceChip как был);
  - состояние: строка → набор (ArrayList<String> из easy/medium/hard,
    rememberSaveable на главном экране);
  - пустой набор + «Собрать тест» / «Сгенерировать» (обе вкладки) →
    Toast «Выберите хотя бы одну сложность», действие отменяется
    (валидация стоит ДО всех последующих сценариев, включая «Добрать
    и собрать»);
  - промпт к ИИ: одна сложность — просим именно её; несколько —
    перечисляем разрешённые и просим распределить примерно поровну;
  - после получения ответа модели каждому вопросу проставляется
    одна из выбранных сложностей случайно (difficulties.random());
  - DraftBuilder.filterQuestions принимает Collection<String>:
    вопрос проходит, если его difficulty входит в набор («all»
    больше не существует);
  - вызовы в доборе теперь передают набор с главного экрана
    (operation «topup» сохранён);
  - форма ручного вопроса/редактирования и форма ручной замены —
    НЕ тронуты (там честная одиночная сложность вопроса).
- app/build.gradle.kts: versionCode = 36, versionName = "0.26-step26".
- Каскад моделей, парсер JSON, UpdaterClient, журнал ИИ — не тронуты.
  Новых библиотек нет.

## Известные проблемы
—
## Заметки
- Путь к локальному APK:
  C:\Users\Ivan\Desktop\TestGenerator\apk\ТестоГен-v0.26-step26.apk
- Версионирование: versionCode монотонно растёт (шаг 26 = 36).
- Автопубликация: тег v36 → GitHub Actions → релиз; у пользователей
  с версией ≤ 35 приложение предложит обновление (36 > 35).
- VISION.md и PLAN.md не менялись.
