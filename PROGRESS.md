# PROGRESS

## Текущий шаг: 28 — «Мои учебники» переехали на вкладку «Файл»
## Статус: собран, тег v39 отправлен, ждёт проверки пользователем

## Что сделано
- ЧАСТЬ 1 — вкладка «Файл» в банке вопросов теперь показывает
  «Мои учебники» (новая технология Шага 27):
  - старое содержимое удалено полностью: кнопка «Выбрать
    PDF-файл», карточка файла (страницы/символы/«×»), поле
    «Тема/класс», счётчик, чипсы сложности, «Сгенерировать
    вопросы» и вся старая логика (runPdfGeneration, pdfPicker,
    состояния pdfFileName/pdfText/pdfPageCount/pdfTopic/…);
  - вместо них: список учебников из Room (Flow) — название,
    формат, число параграфов, дата; пустое состояние «Пока нет
    учебников. Нажмите «+»…»; полоса прогресса при обработке;
  - FAB «+» в правом нижнем углу (на этой вкладке вместо «+»
    банка) → системный выбор PDF/DOCX/TXT → проверка <50 МБ →
    извлечение → парсинг § → Storage → таблицы Supabase → кэш
    Room. Логика полностью переиспользована из Шага 27
    (TextbookRepository), не переписывалась;
  - тап по учебнику → «Выбор диапазона параграфов»
    (textbook_range/{id} — без изменений);
  - долгий тап → удаление с подтверждением (Supabase + Room);
  - вкладка осталась называться «Файл».
- ЧАСТЬ 2 — Настройки: карточка «Учебники» удалена вместе с
  параметром onOpenTextbooks; ссылок на «Мои учебники» из
  Настроек больше нет. Маршрут "textbooks" и экран
  TextbooksScreen оставлены в коде (по заданию), маршрут
  "textbook_range" — как был.
- QuestionBankScreen принял параметр onOpenTextbookRange;
  у вкладки «Файл» контент в собственном LazyColumn с weight,
  FAB контекстный (файл → загрузка учебника, остальные → «+»).
- app/build.gradle.kts: versionCode = 39, versionName = "0.28-step28".
- Вкладки «Ручной ввод» и «Интернет», вход, журнал ИИ,
  автообновление, Room-схема, каскад, парсер — не тронуты.

## Известные проблемы
—
## Заметки
- Путь к локальному APK:
  C:\Users\Ivan\Desktop\TestGenerator\apk\ТестоГен-v0.28-step28.apk
- Версионирование: versionCode монотонно растёт (шаг 28 = 39).
- Автопубликация: тег v39 → GitHub Actions → релиз; телефоны
  с версией ≤ 38 получат предложение обновиться.
- VISION.md и PLAN.md не менялись.
