# PROGRESS

## Текущий шаг: 30 — телефон главный, облако — бэкап (local-first)
## Статус: собран, тег v45 отправлен, ждёт проверки пользователем

## Что сделано
- ЧАСТЬ 1 — Room (версия 7 → 9, две миграции):
  - MIGRATION_7_8: новые таблицы paragraphs (id, textbookId,
    number, title, text, startPage, endPage + индекс) и
    pending_deletes (textbookId, storagePath, userId, createdAt);
  - MIGRATION_8_9: у textbooks добавлены syncStatus
    (старым записям — «synced», они уже в облаке) и userId;
  - существующие учебники/вопросы/настройки сохраняются.
- ЧАСТЬ 2 — загрузка local-first (TextbookRepository):
  1) файл копируется в filesDir/textbooks/<uuid>.<ext>;
  2) извлечение текста + парсинг § (как в шаге 29);
  3) всё сохраняется в Room (Textbook + параграфы) со
     статусом pending_upload;
  4) учебник появляется в списке СРАЗУ;
  5) фоном пробуется отправка в Supabase (Storage upsert,
     textbooks upsert, параграфы перезаливаются начисто);
     успех → synced; неудача (нет сети) → остаётся
     pending_upload, в списке видно «ожидает отправки в
     облако»; без интернета всё работает локально.
- ЧАСТЬ 3 — удаление local-first: Room (параграфы + учебник)
  и локальный файл удаляются сразу; запись кладётся в очередь
  pending_deletes и фоном отправляется в Supabase (таблицы +
  Storage); при неудаче остаётся в очереди.
- ЧАСТЬ 4 — восстановление: при первом входе, если в облаке
  учебников больше, чем на устройстве — диалог «Найдено N
  учебников в облаке. Восстановить…» («Восстановить» /
  «Не сейчас»); в Настройках — кнопка «Восстановить из
  облака» с подтверждением. Восстановление тянет метаданные,
  параграфы и файлы (файл — downloadAuthenticated).
- ЧАСТЬ 5 — фоновая синхронизация: при старте (если
  авторизован) и при открытии вкладки «Файл» — очередь
  удалений + все pending_upload; статусы видны в списке
  («ожидает отправки в облако»); в Настройках блок
  «Синхронизация с облаком: N в очереди» / «Всё
  синхронизировано».
- ЧАСТЬ 6 — генерация из параграфов теперь читает ИЗ ROOM:
  экран диапазона работает полностью офлайн (выбор по
  галочкам/страницам, тема, каскад — как в шаге 29).
- app/build.gradle.kts: versionCode = 45, versionName = "0.30-step30".
- Supabase-интеграция сохранена (как бэкап); вход, каскад,
  автообновление, банк/черновик/экспорт — не тронуты.
  Новых библиотек нет.

## Известные проблемы
—
## Заметки
- Путь к локальному APK:
  C:\Users\Ivan\Desktop\TestGenerator\apk\ТестоГен-v0.30-step30.apk
- Версионирование: versionCode монотонно растёт (шаг 30 = 45).
- Автопубликация: тег v45 → GitHub Actions → релиз.
- Старые учебники из Шага 27 после обновления получают статус
  «synced» (они уже в облаке) и продолжают работать.
- VISION.md и PLAN.md не менялись.
